import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        flex:1,
        height: 56,
        backgroundColor: '#262626',
        borderRadius: 8,
        padding: 16,
        fontSize: 18,
        color: '#fff',
    }
})